#include<iostream>
using namespace std;
   int main () {
       int length = 15 ;
	   int width = 28 ;
	   int area = length * width ;
	   int  perimeter = ( 2 * length ) + (2* width) ;
	   cout << " The area of of basketball " << length * width << endl;
	     cout << " the perimeter of basketabll " << ( 2 * length ) + (2* width) << endl;
		 
		return 0; 
		
		
		}